name: Build and publish llmfit wheels

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Upstream llmfit release tag (e.g. v0.8.6)'
        required: true

jobs:
  # ------------------------------------------------------------------
  # Build all platform wheels
  # ------------------------------------------------------------------
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Build platform wheels
        run: python build_wheels.py --version "${{ inputs.version }}" --output-dir dist/

      - name: List built artifacts
        run: ls -lh dist/

      - uses: actions/upload-artifact@v7
        with:
          name: wheels
          path: dist/
          if-no-files-found: error

  # ------------------------------------------------------------------
  # Install the glibc x86_64 wheel and run the test suite
  # ------------------------------------------------------------------
  test:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - uses: actions/download-artifact@v8
        with:
          name: wheels
          path: dist/

      - name: Install dev environment with built wheel
        run: |
          uv sync --no-install-package llmfit
          uv pip install --find-links dist/ llmfit

      - name: Run tests
        run: uv run pytest

  # ------------------------------------------------------------------
  # Publish to PyPI via OIDC trusted publisher (no API tokens needed)
  # ------------------------------------------------------------------
  publish:
    needs: [build, test]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/llmfit/
    permissions:
      id-token: write   # Required for OIDC

    steps:
      - uses: actions/download-artifact@v8
        with:
          name: wheels
          path: dist/

      - name: List artifacts to publish
        run: ls -lh dist/

      - uses: pypa/gh-action-pypi-publish@release/v1
        # No username/password needed; authentication is via OIDC trusted publisher.
        # Configure trusted publisher at pypi.org/manage/project/llmfit/settings/publishing/
        #   Repository: JEHoctor/llmfit-pypi
        #   Workflow:   build_and_publish.yml
        #   Environment: pypi
